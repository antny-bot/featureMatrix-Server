{
  "name": "feature-matrix",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build-esbuild.js",
    "build:legacy": "node build.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
